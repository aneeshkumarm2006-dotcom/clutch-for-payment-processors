# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# local dev image uploads (lib/upload.ts disk fallback)
/public/uploads

# vercel
.vercel

# indexnow sitemap snapshot (written by `indexnow:ping --sitemap-diff`; the
# real one lives in the GitHub Actions cache, never in the repo)
/.indexnow

# typescript
*.tsbuildinfo
next-env.d.ts.bak
